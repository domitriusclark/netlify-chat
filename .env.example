# Environment Variables Example
# Copy this to .env for local development

# Database Configuration (LibSQL/Turso)
# For local development, use file-based database:
DATABASE_URL="file:./chat-memory.db"

# For production with Turso:
# 1. Sign up at https://turso.tech
# 2. Create database: turso db create netlify-chat-memory
# 3. Get URL: turso db show netlify-chat-memory --url
# 4. Get token: turso db tokens create netlify-chat-memory
# DATABASE_URL="libsql://your-db-name.turso.io"
# DATABASE_AUTH_TOKEN="your-auth-token"

# ========================================
# Netlify AI Gateway
# ========================================
# AI provider credentials are AUTOMATICALLY provided by Netlify AI Gateway
# when running via "netlify dev" or in production.
#
# Requirements:
# 1. Deploy to production at least once (AI Gateway activates after first deploy)
# 2. Use "netlify dev" for local development (NOT "npm run dev")
#
# The following environment variables are injected automatically:
# - AI_GATEWAY (JSON config)
# - OPENAI_API_KEY & OPENAI_BASE_URL
# - ANTHROPIC_API_KEY & ANTHROPIC_BASE_URL
# - GEMINI_API_KEY & GOOGLE_GEMINI_BASE_URL
#
# DO NOT manually configure these variables - let Netlify AI Gateway handle them.
